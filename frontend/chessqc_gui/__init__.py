"""CHESS-QC desktop GUI driver (PySide6).

A single generic calculator shell that renders any chessqc_* application from its
declarative contract (APP_META / INPUTS / OUTPUTS / compute). The compute
applications never import this package; this package imports them.
"""
